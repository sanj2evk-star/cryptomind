services:
  - type: web
    name: cryptomind
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python run_api.py --host 0.0.0.0 --port $PORT
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: API_USERNAME
        value: admin
      - key: API_PASSWORD
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: PYTHON_VERSION
        value: "3.11.0"
    healthCheckPath: /health
    autoDeploy: true
    plan: free
